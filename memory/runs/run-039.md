# Run 039

## Run 39 — stdout

---

## Run 49 Research Summary

**Thread investigated:** `gallopCount` wiring — traced the full gallop signal from data ingestion to UI flag.

### What I found

**Production bug — `gallopRisk` confidence flag is permanently silent:**

The chain breaks at `horseProcessing.ts`. It computes `gallopRate` (career float) and returns it, but the `HorseRawKmTime` interface also declares `gallopCount` and `disqualificationCount` (integer counts for last 10 starts). These are never populated. `confidenceFlags.ts:128` reads `rawTimeData?.gallopCount ?? 0` — always gets `0` — so `gallopRisk` is always `f
